# Testing Notes

## Purpose

This document explains the automated testing added to the RetailOps Insight project.

The goal of testing is to confirm that KPI calculations and cleaned output files behave as expected.

## Testing Tool

The project uses `pytest`.

`pytest` is used to run automated checks from the terminal.

## Test Files

```text
tests/test_kpi_calculations.py
tests/test_cleaned_outputs.py